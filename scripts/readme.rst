This folder is for python scripts to make things easier.

update_models.py
----------------
Scans the models directory and adds a model_t declaration to the models.h file
for each of the subdirectories.